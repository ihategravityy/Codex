const toast=document.querySelector('#toast'),count=document.querySelector('#new-count');document.querySelectorAll('.acknowledge').forEach(button=>button.addEventListener('click',()=>{const alert=document.querySelector(`#${button.dataset.alert}`);alert.querySelector('.priority').className='priority green';alert.querySelector('.priority b').textContent='ACKNOWLEDGED';alert.querySelector('.alert-actions').innerHTML='<p class="assigned">✓ Trauma Team B assigned · Response started</p>';count.textContent=Math.max(0,Number(count.textContent)-1);toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),3500)}));
